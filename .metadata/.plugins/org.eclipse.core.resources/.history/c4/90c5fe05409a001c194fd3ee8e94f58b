package task;

import java.util.ArrayList;
import java.util.List;

public class PersonList {
	
	public static List<Person> people = new ArrayList<Person>();
	
	public String searchPeeps(String p1) {
		PersonList.people.stream().forEach(p -> p.getName().filter({if(p.equalsIgnoreCase(p1)) System.out.println(name);});
		return
	}

}

